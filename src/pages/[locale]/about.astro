---
import AboutPage from '../../components/pages/AboutPage.astro';

export function getStaticPaths() {
  return [
    { params: { locale: 'ar' } },
    { params: { locale: 'ru' } },
    { params: { locale: 'fr' } },
  ];
}

const { locale } = Astro.params;
---

<AboutPage lang={locale} />
