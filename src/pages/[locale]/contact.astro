---
import ContactPage from '../../components/pages/ContactPage.astro';

export function getStaticPaths() {
  return [
    { params: { locale: 'ar' } },
    { params: { locale: 'ru' } },
    { params: { locale: 'fr' } },
  ];
}

const { locale } = Astro.params;
---

<ContactPage lang={locale} />
